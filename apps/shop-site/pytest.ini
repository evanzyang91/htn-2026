# This app owns its own test config on purpose. Without a config file here,
# pytest walks up to the repository root pyproject.toml and inherits its
# testpaths, addopts and import mode, which do not apply to this standalone app.
[pytest]
testpaths = tests
pythonpath = tests
addopts = -q
